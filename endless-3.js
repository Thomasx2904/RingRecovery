'use strict';
const joy={x:0,y:0,id:null};function joyMove(e){let r=ui.joy.getBoundingClientRect(),x=e.clientX-(r.left+r.width/2),y=e.clientY-(r.top+r.height/2),m=Math.hypot(x,y),max=42;if(m>max){x=x/m*max;y=y/m*max}joy.x=x/max;joy.y=y/max;ui.stick.style.transform=`translate(${x}px,${y}px)`}ui.joy.addEventListener('pointerdown',e=>{joy.id=e.pointerId;ui.joy.setPointerCapture(e.pointerId);joyMove(e)});ui.joy.addEventListener('pointermove',e=>{if(e.pointerId===joy.id)joyMove(e)});function joyEnd(e){if(e.pointerId===joy.id){joy.id=null;joy.x=joy.y=0;ui.stick.style.transform=''}}ui.joy.addEventListener('pointerup',joyEnd);ui.joy.addEventListener('pointercancel',joyEnd);ui.dash.addEventListener('pointerdown',e=>{e.preventDefault();dash()});
addEventListener('keydown',e=>{let k=e.key.toLowerCase();if(k===' '){e.preventDefault();dash()}if(k==='w'||k==='arrowup')joy.y=-1;if(k==='s'||k==='arrowdown')joy.y=1;if(k==='a'||k==='arrowleft')joy.x=-1;if(k==='d'||k==='arrowright')joy.x=1});addEventListener('keyup',e=>{let k=e.key.toLowerCase();if(['w','s','arrowup','arrowdown'].includes(k))joy.y=0;if(['a','d','arrowleft','arrowright'].includes(k))joy.x=0});
document.addEventListener('visibilitychange',()=>{if(document.hidden&&state!=='title')paused=true});
